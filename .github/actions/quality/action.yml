name: Quality checks
description: Run clippy and tests

runs:
  using: composite
  steps:
    - run: rustup component add clippy
      shell: bash
    - run: cargo clippy -- -D warnings
      shell: bash
    - run: cargo test
      shell: bash
